spring.application.name=service-statistics

server.port=${SERVER_PORT:8085}

spring.data.mongodb.uri=${MONGODB_URI:mongodb://localhost:27017/techcup_statistics}

# URL base del servicio de Torneos, usada para preguntar cual es el torneo
# activo. NOTA: ese endpoint (GET /tournaments/active) todavia no existe en
# el servicio real de Torneos (verificado 2026-07-14) - pendiente de que lo
# agreguen o de rediseñar este flujo. Puerto 8080 confirmado desde su
# docker-compose.yml.
services.tournaments.base-url=${TOURNAMENTS_SERVICE_URL:http://localhost:8080}
